<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auction.repository.MemberMapper">
	<!-- 회원가입 -->
	<insert id="saveMember">
		insert into member(
			member_id,
			password,
			username,
			birth,
			gender,
			address_1,
			address_2,
			address_3,
			email,
			phone
		) values (
			#{member_id},
			#{password},
			#{username},
			#{birth},
			#{gender},
			#{address_1},
			#{address_2},
			#{address_3},
			#{email},
			#{phone}
		)
	</insert>
	
	<select id="findMember" resultType="member">
		select * from member where member_id = #{member_id}
	</select>
</mapper>